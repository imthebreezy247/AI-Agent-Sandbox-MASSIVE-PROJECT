version: "3.9"

services:
  sandbox:
    build: .
    ports:
      - "8000:8000"
    environment:
      - MAX_AGENTS=8
      - SANDBOX_TIMEOUT=300
      - LOG_LEVEL=INFO
    volumes:
      - sandbox-data:/app/sandboxes
    restart: unless-stopped

volumes:
  sandbox-data:
